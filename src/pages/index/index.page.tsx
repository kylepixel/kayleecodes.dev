import { Root } from './index.styles';

export const Page = () => {
    return (
        <Root>
            <h1>Welcome</h1>
            This page is:
            <ul>
                <li>Rendered to HTML.</li>
            </ul>
        </Root>
    );
};
